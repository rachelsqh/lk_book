linux 通用linux系统控制接口:sysctl
----------------------------------
.. code-block:: c
   :caption: struct_task --> mm
   :emphasize-lines: 4,5
   :linenos:

对各种接口进行下总结
CONFIG_SYSCTL:

1. 增加/proc支持
2. bdflush入口和intvec min/max校验
3. /proc/sys/net

